export type CultureTextOptions = {
  theme: string;
  element: string;
  product: string;
  meaning: string;
  gridSize: number;
  colorCount: number;
};

export type CultureCopy = {
  title: string;
  source: string;
  meaning: string;
  design: string;
  scenario?: string;
  steps?: string[];
};
